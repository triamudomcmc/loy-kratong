import type { NextPage } from "next";

export interface Selected {
  base: string;
  swan: string;
  candles: string;
  decorations: string;
}

export const PrincipalKratong: NextPage = () => {
  return <></>;
};
