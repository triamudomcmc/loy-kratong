import type { NextPage } from "next";

export interface Selected {
  base: string;
  swan: string;
  candles: string;
  decorations: string;
  offset: number;
}

export const PrincipalKratong: NextPage = () => {
  return <></>;
};
